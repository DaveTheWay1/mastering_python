
# * Iteration
# The items in tuples are iterated over by using 
# for...in loops as we saw previously with lists:
colors = ('red', 'green', 'blue')
for idx, color in enumerate(colors):
  print(idx, color)
# > 0 red
# > 1 green
# > 2 blue